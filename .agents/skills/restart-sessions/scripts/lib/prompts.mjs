// 再起動した新セッションに、最初のプロンプトで渡す進め方。2つのスクリプトが同じ文を送るので、ここ1つに置く。
//
// 再起動はコンテキストを入れ替えるもので、セッションがしていたことは変えない。進めていたなら続きから進め、答えを待っていたなら同じ問いで待つ。
// バックグラウンドの処理は再起動で止まるので、資料に書かれた手順で立ち上げ直させる。
// 状態は資料から読む。資料を書く側には、手順2の依頼文と自己再起動の節で、再起動の前の状態を書かせている。
// 資料は会話の要約なので、プロジェクトの大きな流れは別の置き場にある。資料が指していれば、続ける前に読ませる。
// 承認は資料では運べないので、後戻りできない操作と外向きの操作は、新セッションが確かめ直す。
// ユーザーが記憶や CLAUDE.md で前もって決めた扱いは、新セッションにもそのまま見えるので、それに従わせる。
export const HOW_TO_PROCEED =
  '資料に書かれた前セッションの状態（再起動の依頼を受ける前の状態）をそのまま引き継ぐこと。' +
  '資料がプロジェクトの全体の流れの置き場を指していれば、続ける前にそれも読む。' +
  '資料に立ち上げ直す手順が書かれたバックグラウンドの処理は、どの状態でも先に立ち上げ直す。' +
  '作業の途中（ユーザーの新しい指示が無くても次の一手に進むつもりだった）なら、その続きから再開する。' +
  'ユーザーの答えを待っていたなら、同じ問いを出し直して待つ（選択式の問いだったなら AskUserQuestion で出し直す）。答えが返るまでは、現状を確かめる読み取りだけにとどめる。' +
  '指示待ち（次に何をするかをユーザーが決める）なら、そのまま待つ。' +
  '資料から状態を読み取れなければ、推し量って進めずにユーザーに聞く。' +
  '後戻りできない操作や外向きの操作（push・PR・マージ・削除・他セッションへの依頼など）は、資料に承認済みとあっても、改めて確認する。' +
  'ただし、ユーザーが記憶や CLAUDE.md などで前もって決めている扱いがあれば、厳しくても緩くても、そちらに従う。';
